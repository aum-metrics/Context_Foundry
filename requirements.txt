# AUM: Unified Streamlit Cloud Dependencies - FINAL RESOLUTION

# ---- Core UI (Pinned for stability) ----
streamlit==1.40.1
pandas==2.2.3
numpy==1.26.4
plotly==5.24.1
openpyxl==3.1.5
requests==2.31.0

# ---- NLP & Embeddings (Requires Python 3.11) ----
sentence-transformers==2.5.1
torch==2.2.0
transformers==4.38.0

# ---- Supabase/Networking FIX ----
# Rollback to 1.0.0 to avoid 'proxy' error
supabase==1.0.0 
# CRITICAL FIX: storage3 must be 0.5.0 for supabase 1.0.0
storage3==0.5.0
tqdm==4.66.5

# ---- Database/Visualization ----
duckdb==0.10.0
matplotlib==3.8.2
seaborn==0.13.2
altair==5.3.0
