# AUM: Unified Streamlit Cloud Dependencies - Final Stable Configuration

# ---- Core UI (Pinned for stability) ----
streamlit==1.40.1
pandas==2.2.3
numpy==1.26.4
plotly==5.24.1
openpyxl==3.1.5
requests==2.31.0

# ---- NLP & Embeddings (Requires Python 3.11) ----
sentence-transformers==2.5.1
torch==2.2.0
transformers==4.38.0

# ---- Supabase/Networking FIX (Rollback to Known Stable V1) ----
# This rollback is the only reliable way to bypass the 'proxy' error on Streamlit Cloud
supabase==1.0.0
storage3==0.6.0
tqdm==4.66.5

# ---- Database/Visualization ----
duckdb==0.10.0
matplotlib==3.8.2
seaborn==0.13.2
altair==5.3.0
